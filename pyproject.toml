[project]
name = "jobagent"
version = "0.1.0"
description = "MCP server for managing a personal job-application workflow."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.12",
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
